//creo un ciclo per far ripetere 5 volte la stessa domanda, stampando i risultati della variabile
for (var i = 0; i < 5; i++) {
    var lista_numeri = parseInt(prompt("inserisci un numero"))
    console.log(lista_numeri)
}
